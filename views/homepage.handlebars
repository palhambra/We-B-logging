{{#each blogs as |blog| }}
<a href="/blog/{{id}}">
<div class="row align-center mb-5">
  <div class="col-md-12">
    <h2> {{title}} </a> <p> By: {{username}} on {{format_date date_created}}</p></h2>
    <p> {{content}}</p>
  </div>
</div>


{{/each}}