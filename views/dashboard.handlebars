<form style="margin-bottom: 50px;" action="/dashboard" method="POST">
    <label>Title:</label>
    <input style="width:100%;" type="text" name="title" id="title" required>
    <br>
    <label>Content:</label>
    <input style="width:100%; height:70px;" type="text" name="content" id="content" required>
    <button style="margin:15px; float:right;" type="submit">Post New Blog</button>
</form>



{{#if blogs}}
  {{#each blogs as |blog| }}
    <a href="/blog/{{id}}">
      <div class="row align-center mb-5">
        <div class="col-md-12">
          <h2>{{title}}</h2>
          <p>By: {{username}} on {{format_date date_created}}</p>
          <p>{{content}}</p>
        </div>
      </div>
    </a>
  {{/each}}
{{else}}
  <p>You have not created any blogs yet.</p>
{{/if}}